Questo script ha come scopo principale quello di determinare chi lo sta eseguendo, se root o un utente normale.
Nella prima parte viene fatto un controllo sull'UID, per vedere se è pari a 0, ovvero a quello di ROOT.
Se è 0, viene stampato il messaggio che in quel momento si è root.
Se invece è diverso da 0, viene stampato il messaggio che si è solo un utente ordinario.
Successivamente lo script termina, ma all'interno del file è presente un'altra parte, che svolge la medesima funzione.
Essa non viene eseguita perché si trova dopo l'uscita.
